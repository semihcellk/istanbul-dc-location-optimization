# Core numerics / data
numpy>=1.24
pandas>=2.0

# MILP solver. PuLP ships a bundled CBC binary, which is what PULP_CBC_CMD
# uses; the <4 pin keeps that entry point available.
pulp>=2.7,<4

# Data pipeline (IBB open-data API)
requests>=2.28

# Visualisation
matplotlib>=3.7
seaborn>=0.12
folium>=0.14

# Notebook / interactive dashboard
jupyter>=1.0
ipywidgets>=8.0

# Tests
pytest>=7.0
